variable website_bucket {
    description = "the s3 bucket that gets the iam policy"
}

variable s3_distribution_arn {
    description = "the arn of the cloudfront distribution going on the s3 iam policy"
}